# Cloud-only: no torch, no sentence-transformers, no local model.
chromadb==0.5.23
rank-bm25==0.2.2
pypdf==5.1.0
python-docx==1.1.2
pandas==2.2.3
openpyxl==3.1.5
python-dotenv==1.0.1
requests==2.32.3

# Azure OpenAI
openai==1.59.6

# Agent graph (ADR-011)
langgraph==0.2.60
langchain-core==0.3.29

# HTTP API
fastapi==0.115.6
uvicorn[standard]==0.34.0
# Multipart parsing for the document upload endpoint. FastAPI needs it at
# import time, not request time: without it the app fails to start.
python-multipart==0.0.32
